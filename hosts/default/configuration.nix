# Host-specific configuration
# This file contains settings specific to this machine

{ config, pkgs, ... }:

{
  # You can add host-specific overrides here
  # For example, if you have multiple machines in this repo

  # Uncomment and customize as needed:
  # networking.hostName = "my-desktop";
  # time.timeZone = "America/Los_Angeles";

  # Host-specific packages
  # environment.systemPackages = with pkgs; [
  #   # Add packages specific to this host
  # ];
}
